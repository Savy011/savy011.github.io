<svg
    aria-hidden="true"
    viewbox="0 0 100 100"
    xmlns="http://www.w3.org/2000.svg"
    class="svg-filter absolute"
    style="pointer-events: none;"
>
    <filter id="pretty-colors">
        <feTurbulence 
            type="fractalNoise"
            baseFrequency="0.65"
            numOctaves="3"
            stitchTiles="stitch"
        />
    </filter>
    <rect width="100%" height="100%" filter="url(#pretty-colors)" />
</svg>
